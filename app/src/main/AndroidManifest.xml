<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xq.mymaterialdesign">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/XuQiangStyle">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".coordinate.CoorToolBarActivity" />
        <activity android:name=".coordinate.CoorAppBarActivity" />
        <activity android:name=".coordinate.ScrollingActivity" />
        <activity android:name=".toolbar.ToolbarActivity" />
        <activity android:name=".cardview.CardViewActivity" />
        <activity android:name=".palette.PaletteActivity" />
        <activity android:name=".coordinate.CoorAppBarActivity1" />

    </application>

</manifest>